import React from "react";
import * as functions from "./functions";

class Session extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
            heading: "Session",
            length: 25 * 60,
        };

        functions.decr
    }

}